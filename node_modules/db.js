/**
 * Created by mohammad on 7/5/15.
 */

var knex = require('knex')({
    client: 'sqlite3',
    connection: {
        filename: __dirname + "/document.db3"
    }
});

var bookshelf = require('bookshelf')(knex);

module.exports = bookshelf;